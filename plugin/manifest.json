{
  "schemaVersion": 1,
  "id": "fileri.tikk",
  "name": "tikk",
  "version": "0.1.0",
  "author": "Erik Fillipsveen",
  "license": "MIT",
  "description": "Apple Reminders in the Omarchy bar, through an SSH gateway on a Mac you own. The pill counts open reminders in one list; the panel lets you tick them off and add new ones.",
  "kinds": ["bar-widget"],
  "entryPoints": { "barWidget": "BarWidget.qml" },
  "barWidget": {
    "displayName": "tikk",
    "description": "Open reminders in your list. Dimmed = the Mac gateway is unreachable. Left-click = panel, middle-click = refresh.",
    "category": "Info",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": { "list": "" },
    "schema": [
      { "key": "list", "type": "string", "label": "Reminders list", "description": "Name of the list to show (empty = the first list the Mac reports)" }
    ]
  }
}
